import sys
sys.path.insert(0, '../../../')
import pandas as pd
import sqlite3
from datetime import datetime


class BuyStratAlgo:

	def __init__(self):
		pass

	@staticmethod
	def run_algorithm(data):
		output = []
		assert data

		# todo: run algorithm that looks for a bunch of RSI's following a MACD signal

		return output
